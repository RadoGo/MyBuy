import React, { Component } from 'react';
import {
  AppRegistry,
  StyleSheet,
  Text,
  View,
  Navigator
} from 'react-native';

/**
* 购物车底部计算金额组件(显示:全选,合计金额,总额,去结算按钮)
*/
export default class BuyCarCalcComponent extends Component {
  constructor(props) {
    super(props);
  }
}

const styles = StyleSheet.create({
});
